<% content_for :title, "施設詳細" %>

<div class="md:w-2/3 w-full">
  <%= render "shared/flash_message" %>

  <h1 class="font-bold text-4xl">施設詳細</h1>

  <%= render 'facilities/facility',facility: @facility, show_actions: false %>
  <%= link_to "一覧へ戻る", facilities_path, class: "ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>

  <%# 投稿者がログインユーザーと同じ場合、編集・削除ボタンを表示 %>
  <% if @facility.user == current_user %>
  <%= link_to "投稿を編集する", edit_facility_path(@facility), class: "mt-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
  <div class="inline-block ml-2">
    <%= button_to "投稿を削除する", @facility, method: :delete, class: "mt-2 rounded-md px-3.5 py-2.5 text-white bg-red-600 hover:bg-red-500 font-medium" %>
  <% end %>
  </div>

  <%= render 'comments/form', facility: @facility, comment: @comment %>
  <%= render @comments, facility: @facility %>
</div>
